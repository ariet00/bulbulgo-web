# Tglab cabinet env
#
# NEXT_PUBLIC_API_URL — backend base URL (the API mounts the cabinet at
# <NEXT_PUBLIC_API_URL>/tglab/*). Local backend runs on :8008.
NEXT_PUBLIC_API_URL=http://localhost:8008/api/v1
